#include <stdio.h>

int GetSet( int arr[] ) ;

int main() {
int data[ 100 ], num ;
num = GetSet( data ) ;

printf("Number of elements: %d\n",num) ;
for(int i = 0 ; i < num ; i++ ) {
printf(" %d", data[ i ] ) ;

}//end for

return 0 ;
}//end function

int GetSet( int arr[] ) {
int num ;  

printf( "Enter the number of elements:" ) ;
scanf( "%d", &num ) ;

printf( "Enter the elements:\n" ) ;
for(int i = 0 ; i < num ; i++ ) {
scanf("%d", &arr[ i ] ) ;

}//end for
return num ;

}
